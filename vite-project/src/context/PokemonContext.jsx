// TODO: Create the context

// PokemonContext.jsx
import { createContext } from "react"; // import `createContext` in order to create context.

const PokemonContext = createContext(); // create variable as a reference to the contex.

export default PokemonContext; //export variable to be used in other files.